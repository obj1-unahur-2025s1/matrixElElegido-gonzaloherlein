import matrix.*

describe "Test de Neo" {

  test "Al saltar neo pierde la mitad de su energia" {
    neo.saltar()
    assert.equals(50, neo.energia())
  }

  test "Su vitalidad inicial"{
    assert.equals(10,neo.vitalidad())
  }

  test "Es el elegido" {
    assert.that(neo.esElElegido())
  }

}